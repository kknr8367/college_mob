import 'package:flutter/material.dart';

class TeacherExamsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Exams'),
      ),
      body: Center(
        child: Text('Teacher Exams Screen'),
      ),
    );
  }
}
