import 'package:flutter/material.dart';

class AddStudentAttendanceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Student Attendance'),
      ),
      body: Center(
        child: Text('Add Student Attendance Screen'),
      ),
    );
  }
}
