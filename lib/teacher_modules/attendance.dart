import 'package:flutter/material.dart';

class TeacherAttendanceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Teacher Attendance'),
      ),
      body: Center(
        child: Text('Teacher Attendance Screen'),
      ),
    );
  }
}
