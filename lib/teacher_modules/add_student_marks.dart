import 'package:flutter/material.dart';

class AddStudentMarksScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Student Marks'),
      ),
      body: Center(
        child: Text('Add Student Marks Screen'),
      ),
    );
  }
}
