import 'package:flutter/material.dart';

class TeacherReportsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Reports'),
      ),
      body: Center(
        child: Text('Teacher Reports Screen'),
      ),
    );
  }
}
