import 'package:flutter/material.dart';

class TeacherNotificationsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Notifications'),
      ),
      body: Center(
        child: Text('Teacher Notifications Screen'),
      ),
    );
  }
}
