import 'package:flutter/material.dart';

class ViewStudentDetailsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('View Student Details'),
      ),
      body: Center(
        child: Text('View Student Details Screen'),
      ),
    );
  }
}
