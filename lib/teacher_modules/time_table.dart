import 'package:flutter/material.dart';

class TeacherTimeTableScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Teacher Time-Table'),
      ),
      body: Center(
        child: Text('Teacher Time-Table Screen'),
      ),
    );
  }
}
